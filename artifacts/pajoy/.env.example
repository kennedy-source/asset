# Copy to .env — never commit .env
# Docker/nginx: leave VITE_API_URL empty to proxy /api to the api service
# Local Vite dev: point to the API port

VITE_API_URL=http://localhost:8080
VITE_PAYSTACK_PUBLIC_KEY=pk_test_xxxxxxxx
